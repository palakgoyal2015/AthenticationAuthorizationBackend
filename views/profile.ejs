<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div>
    <h3 class="text-3xl mb-5">Welcome<span class="font-light"> <%= user.name%></span></h3>
    <h4>You can create a new post</h4>
    <form action="/post" method="post">
        <textarea class="block p-3 resize-none bg-transparent text-color border-2 px-3" name="content" id="" placeholder="What's on your mind"></textarea>
        <input class="px-3 py-2 bg-blue-500 rounded-md" type="submit" value="Create">
        
<a class="bg-red-400 text-white px-3 py-2 rounded-md" href="/logout">Logout</a>
    </form>
 <div class="posts mt-20">
    <h3 class="text-zinc-400">Your Posts</h3>
    <div class="postcontainer mt-5">
        <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 border-2 border-zinc-800 rounded-md">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p><%= post.content %></p>
                <div class="btns flex mt-5">
                    <a class="text-blue-500" href="">Like</a>
                    <a class="text-zinc-600" href="">Edit</a>
                </div>
            </div>
      <%  })  %>
     
        
    </div>
 </div>
</div>
</body>
</html>